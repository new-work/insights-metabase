---
apiVersion: v1
kind: Service
metadata:
  name: "<%= name %>"
  namespace: insights
  labels:
    deploymentName: metabase
spec:
  ports:
  - name: metabase
    port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: "<%= name %>"
  type: ClusterIP
